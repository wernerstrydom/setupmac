# Vale prose linter — https://vale.sh
# Run `vale sync` after cloning to download styles.

StylesPath = .vale/styles
MinAlertLevel = warning

Packages = Vale, Google

[*.md]
BasedOnStyles = Vale, Google
